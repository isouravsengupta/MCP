{
  "name": "spi-mcp-vibe",
  "version": "1.0.0",
  "description": "SPI MCP server — Snowflake sales intelligence via Slack, with full SPM + QAP metric catalog and SSO/keypair auth",
  "type": "module",
  "main": "dist/lambda.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "check": "tsc --noEmit",
    "dev": "tsx src/lambda.ts",
    "start": "node dist/lambda.js"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.600.0",
    "@modelcontextprotocol/sdk": "^1.13.1",
    "dotenv": "^17.4.2",
    "snowflake-sdk": "^2.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
